Use $prompt-authoring to formulate one Codex Goal Mode contract.

Goal: Inspect active AGENTS.md, repository/task truth, and named sources. Decide whether the task fits Goal Mode: one durable outcome, larger than one turn, smaller than an open backlog, with a verifiable stop. Otherwise recommend an ordinary task or a split.

Output: Return the fit with one short reason. For `goal`, return one copy-ready English block using exactly these headings: Outcome, Boundaries, Sources, Done, Progress, Stop. Done names the exact commands or artifacts that prove completion. Progress names the checkpoint, verified evidence, remaining work, and any blocker. Apply the skill's behavior-example gate only when triggered. Reference active rules and skills; never copy them. Require re-grounding after resume or compaction.

Stop: Do not implement or create the goal. Ask one question only when a user-owned ambiguity changes the outcome, scope, or Done; otherwise return the contract.

Task:
<PASTE TASK>
