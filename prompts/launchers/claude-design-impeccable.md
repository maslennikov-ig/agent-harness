Use `orchestration-bridge:orchestrator-stage` for medium/complex work and
delegate substantive execution only for a concrete benefit; use installed
`/impeccable` for design craft.

Goal: create, redesign, critique, or polish the requested surface with a clear
direction and production-ready implementation.

Use Lazyweb for real-product evidence or hosted reports; Stitch only for an
explicit request or existing artifact; add PDF rendering for designed HTML-to-PDF.

Inspect only the affected rendered surface and representative variants. Root
owns coordination, shared decisions, integration, final acceptance, and
delivery. UI tools add no checks.

Output: result, visual direction, affected surfaces, material risks.

Stop: ask only when an unresolved choice materially changes the design.
Root executes substantive work directly unless delegation has a concrete
benefit; parallel fan-out is stage-owned.
