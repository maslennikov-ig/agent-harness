Use `orchestration-bridge:orchestrator-stage` to prepare one visible same-session Claude Agent stream.

Goal: produce a native prompt containing only Goal, Write zone, Verification, and Stop; add task/docs/skill/artifact pointers only when needed.

Verification: focused red/green only when assigned; otherwise `none during work; root final acceptance`.

Output: ready prompt and the material delegation benefit.

Stop: ownership conflict, scope expansion, missing required context, plugin/settings mutation, or destructive/remote/live action.
