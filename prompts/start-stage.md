Use $orchestrator-stage for the current Codex task.

Goal: deliver one cohesive outcome with one root orchestrator and one final acceptance boundary.

Context: inspect repository truth and the current task; load only the stage references that the task actually triggers. Apply the skill's substantive-execution ownership and safe parallelism rules, plus its behavior-example gate, only when triggered.

Output: classification, boundary, owned streams if any, exact acceptance intent, and next action.

Stop: ask only on qualified user-owned ambiguity, unsafe scope expansion, ownership conflict, or destructive/remote/live action.
