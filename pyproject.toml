[project]
name = "dash-harness"
version = "0.0.0"
requires-python = ">=3.10"
dependencies = [
  "a2a-sdk[http-server]==1.1.2",
  "starlette==1.6.0",
  "uvicorn==0.52.1",
]

[project.optional-dependencies]
compatibility = [
  "uvicorn==0.52.1",
]

[tool.uv]
package = false
constraint-dependencies = [
  "protobuf==5.29.5",
]
