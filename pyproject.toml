[project]
name = "agent-harness"
version = "0.1.0"
description = "Portable public bootstrap for Codex, Claude, orchestration-console, MCP, Beads, Graphify, and private encrypted overlays."
requires-python = ">=3.11"
dependencies = []

[project.scripts]
harness = "harness_core.cli:main"

