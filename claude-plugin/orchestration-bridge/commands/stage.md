---
description: Launch orchestrator-stage for a task (fast prompt card)
argument-hint: <task description>
---

Use `orchestration-bridge:orchestrator-stage` for the following Claude Code CLI task.

Task: $ARGUMENTS

Apply the skill's canonical tier, cohesive-stage sizing, scope-ledger, evidence-reuse, delegation, safe-parallelism, and closeout contracts without restating them here. Delegate a stream only for a concrete parallel, context-isolation, specialist, or write-isolation benefit; otherwise execute directly. Root retains coordination, shared decisions, integration, final acceptance, and delivery. Return classification, selected boundary, execution decision, verification evidence, and closeout result.
